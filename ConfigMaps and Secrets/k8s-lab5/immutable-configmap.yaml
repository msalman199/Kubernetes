apiVersion: v1
kind: ConfigMap
metadata:
  name: immutable-config
immutable: true
data:
  config.yaml: |
    app:
      name: "MyApp"
      version: "1.0.0"
      environment: "production"
    database:
      host: "prod-db.example.com"
      port: 5432
